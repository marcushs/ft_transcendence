import { UserProfile } from "./DisplayUserProfile.js";

export default () => {
    const html = `
        <section class="profile-page"></section>
    `;

	setTimeout(() =>{

	}, 0);
    return html;
}